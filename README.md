

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

3. admin email:
   `admin@bosc.library`
   
admin password:
   `adminpassword`